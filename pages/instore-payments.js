import Head from "next/head";
import React, { Component } from "react";
import Features from "../components/InstorePayments/Features";
import Cta from "../components/InstorePayments/Cta";
import Section from "../components/InstorePayments/section";
import Pricing from "../components/InstorePayments/Pricing";
import News from "../components/InstorePayments/News";
import Footer from "../components/Layout/Footer";
import Topbar from "../components/Layout/Topbar";
//Render Components

class payment extends Component {
  componentDidMount() {
    document.body.classList = "";
    document.getElementById("topnav")?.classList.add("bg-white");
    window.addEventListener("scroll", this.scrollNavigation, true);
  }
  // Make sure to remove the DOM listener when the component is unmounted.
  componentWillUnmount() {
    window.removeEventListener("scroll", this.scrollNavigation, true);
  }

  scrollNavigation = () => {
    var doc = document.documentElement;
    var top = (window.pageYOffset || doc.scrollTop) - (doc.clientTop || 0);
    if (top > 80) {
      document.getElementById("topnav")?.classList.add("nav-sticky");
    } else {
      document.getElementById("topnav")?.classList.remove("nav-sticky");
    }
  };
  render() {
    return (
      <div>
        <Head>
          <meta name="description" content="Generated by create next app" />
        </Head>

        <main>
          <Topbar />
          <Section />
          {/* Feature */}
          <Features />

          {/* Cta */}
          <Cta />

          {/* Pricing */}
          <Pricing />

          {/* News */}
          <News />
          <Footer />
        </main>
      </div>
    );
  }
}

export default payment;
